<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lagou.mapper.BOrderMapper">
    <insert id="addBOrder" parameterType="com.lagou.pojo.BOrder">
        insert into b_order (company_id,user_id,name) values(#{companyId},#{userId},#{name})
    </insert>
</mapper>